export const { bookmarks } = window.chrome
export const { commands } = window.chrome
export const { extension } = window.chrome
export const { runtime } = window.chrome
export const { windows } = window.chrome
